// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/in-toto/archivista/ent/predicate"
	"github.com/in-toto/archivista/ent/statement"
	"github.com/in-toto/archivista/ent/vexdocument"
	"github.com/in-toto/archivista/ent/vexstatement"
)

// VexDocumentQuery is the builder for querying VexDocument entities.
type VexDocumentQuery struct {
	config
	ctx               *QueryContext
	order             []vexdocument.OrderOption
	inters            []Interceptor
	predicates        []predicate.VexDocument
	withVexStatements *VexStatementQuery
	withStatement     *StatementQuery
	withFKs           bool
	modifiers         []func(*sql.Selector)
	loadTotal         []func(context.Context, []*VexDocument) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the VexDocumentQuery builder.
func (vdq *VexDocumentQuery) Where(ps ...predicate.VexDocument) *VexDocumentQuery {
	vdq.predicates = append(vdq.predicates, ps...)
	return vdq
}

// Limit the number of records to be returned by this query.
func (vdq *VexDocumentQuery) Limit(limit int) *VexDocumentQuery {
	vdq.ctx.Limit = &limit
	return vdq
}

// Offset to start from.
func (vdq *VexDocumentQuery) Offset(offset int) *VexDocumentQuery {
	vdq.ctx.Offset = &offset
	return vdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (vdq *VexDocumentQuery) Unique(unique bool) *VexDocumentQuery {
	vdq.ctx.Unique = &unique
	return vdq
}

// Order specifies how the records should be ordered.
func (vdq *VexDocumentQuery) Order(o ...vexdocument.OrderOption) *VexDocumentQuery {
	vdq.order = append(vdq.order, o...)
	return vdq
}

// QueryVexStatements chains the current query on the "vex_statements" edge.
func (vdq *VexDocumentQuery) QueryVexStatements() *VexStatementQuery {
	query := (&VexStatementClient{config: vdq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := vdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := vdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(vexdocument.Table, vexdocument.FieldID, selector),
			sqlgraph.To(vexstatement.Table, vexstatement.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, vexdocument.VexStatementsTable, vexdocument.VexStatementsColumn),
		)
		fromU = sqlgraph.SetNeighbors(vdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryStatement chains the current query on the "statement" edge.
func (vdq *VexDocumentQuery) QueryStatement() *StatementQuery {
	query := (&StatementClient{config: vdq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := vdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := vdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(vexdocument.Table, vexdocument.FieldID, selector),
			sqlgraph.To(statement.Table, statement.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, vexdocument.StatementTable, vexdocument.StatementColumn),
		)
		fromU = sqlgraph.SetNeighbors(vdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first VexDocument entity from the query.
// Returns a *NotFoundError when no VexDocument was found.
func (vdq *VexDocumentQuery) First(ctx context.Context) (*VexDocument, error) {
	nodes, err := vdq.Limit(1).All(setContextOp(ctx, vdq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{vexdocument.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (vdq *VexDocumentQuery) FirstX(ctx context.Context) *VexDocument {
	node, err := vdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first VexDocument ID from the query.
// Returns a *NotFoundError when no VexDocument ID was found.
func (vdq *VexDocumentQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = vdq.Limit(1).IDs(setContextOp(ctx, vdq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{vexdocument.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (vdq *VexDocumentQuery) FirstIDX(ctx context.Context) int {
	id, err := vdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single VexDocument entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one VexDocument entity is found.
// Returns a *NotFoundError when no VexDocument entities are found.
func (vdq *VexDocumentQuery) Only(ctx context.Context) (*VexDocument, error) {
	nodes, err := vdq.Limit(2).All(setContextOp(ctx, vdq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{vexdocument.Label}
	default:
		return nil, &NotSingularError{vexdocument.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (vdq *VexDocumentQuery) OnlyX(ctx context.Context) *VexDocument {
	node, err := vdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only VexDocument ID in the query.
// Returns a *NotSingularError when more than one VexDocument ID is found.
// Returns a *NotFoundError when no entities are found.
func (vdq *VexDocumentQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = vdq.Limit(2).IDs(setContextOp(ctx, vdq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{vexdocument.Label}
	default:
		err = &NotSingularError{vexdocument.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (vdq *VexDocumentQuery) OnlyIDX(ctx context.Context) int {
	id, err := vdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of VexDocuments.
func (vdq *VexDocumentQuery) All(ctx context.Context) ([]*VexDocument, error) {
	ctx = setContextOp(ctx, vdq.ctx, "All")
	if err := vdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*VexDocument, *VexDocumentQuery]()
	return withInterceptors[[]*VexDocument](ctx, vdq, qr, vdq.inters)
}

// AllX is like All, but panics if an error occurs.
func (vdq *VexDocumentQuery) AllX(ctx context.Context) []*VexDocument {
	nodes, err := vdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of VexDocument IDs.
func (vdq *VexDocumentQuery) IDs(ctx context.Context) (ids []int, err error) {
	if vdq.ctx.Unique == nil && vdq.path != nil {
		vdq.Unique(true)
	}
	ctx = setContextOp(ctx, vdq.ctx, "IDs")
	if err = vdq.Select(vexdocument.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (vdq *VexDocumentQuery) IDsX(ctx context.Context) []int {
	ids, err := vdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (vdq *VexDocumentQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, vdq.ctx, "Count")
	if err := vdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, vdq, querierCount[*VexDocumentQuery](), vdq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (vdq *VexDocumentQuery) CountX(ctx context.Context) int {
	count, err := vdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (vdq *VexDocumentQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, vdq.ctx, "Exist")
	switch _, err := vdq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (vdq *VexDocumentQuery) ExistX(ctx context.Context) bool {
	exist, err := vdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the VexDocumentQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (vdq *VexDocumentQuery) Clone() *VexDocumentQuery {
	if vdq == nil {
		return nil
	}
	return &VexDocumentQuery{
		config:            vdq.config,
		ctx:               vdq.ctx.Clone(),
		order:             append([]vexdocument.OrderOption{}, vdq.order...),
		inters:            append([]Interceptor{}, vdq.inters...),
		predicates:        append([]predicate.VexDocument{}, vdq.predicates...),
		withVexStatements: vdq.withVexStatements.Clone(),
		withStatement:     vdq.withStatement.Clone(),
		// clone intermediate query.
		sql:  vdq.sql.Clone(),
		path: vdq.path,
	}
}

// WithVexStatements tells the query-builder to eager-load the nodes that are connected to
// the "vex_statements" edge. The optional arguments are used to configure the query builder of the edge.
func (vdq *VexDocumentQuery) WithVexStatements(opts ...func(*VexStatementQuery)) *VexDocumentQuery {
	query := (&VexStatementClient{config: vdq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	vdq.withVexStatements = query
	return vdq
}

// WithStatement tells the query-builder to eager-load the nodes that are connected to
// the "statement" edge. The optional arguments are used to configure the query builder of the edge.
func (vdq *VexDocumentQuery) WithStatement(opts ...func(*StatementQuery)) *VexDocumentQuery {
	query := (&StatementClient{config: vdq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	vdq.withStatement = query
	return vdq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		VexID string `json:"vex_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.VexDocument.Query().
//		GroupBy(vexdocument.FieldVexID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (vdq *VexDocumentQuery) GroupBy(field string, fields ...string) *VexDocumentGroupBy {
	vdq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &VexDocumentGroupBy{build: vdq}
	grbuild.flds = &vdq.ctx.Fields
	grbuild.label = vexdocument.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		VexID string `json:"vex_id,omitempty"`
//	}
//
//	client.VexDocument.Query().
//		Select(vexdocument.FieldVexID).
//		Scan(ctx, &v)
func (vdq *VexDocumentQuery) Select(fields ...string) *VexDocumentSelect {
	vdq.ctx.Fields = append(vdq.ctx.Fields, fields...)
	sbuild := &VexDocumentSelect{VexDocumentQuery: vdq}
	sbuild.label = vexdocument.Label
	sbuild.flds, sbuild.scan = &vdq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a VexDocumentSelect configured with the given aggregations.
func (vdq *VexDocumentQuery) Aggregate(fns ...AggregateFunc) *VexDocumentSelect {
	return vdq.Select().Aggregate(fns...)
}

func (vdq *VexDocumentQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range vdq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, vdq); err != nil {
				return err
			}
		}
	}
	for _, f := range vdq.ctx.Fields {
		if !vexdocument.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if vdq.path != nil {
		prev, err := vdq.path(ctx)
		if err != nil {
			return err
		}
		vdq.sql = prev
	}
	return nil
}

func (vdq *VexDocumentQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*VexDocument, error) {
	var (
		nodes       = []*VexDocument{}
		withFKs     = vdq.withFKs
		_spec       = vdq.querySpec()
		loadedTypes = [2]bool{
			vdq.withVexStatements != nil,
			vdq.withStatement != nil,
		}
	)
	if vdq.withStatement != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, vexdocument.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*VexDocument).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &VexDocument{config: vdq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(vdq.modifiers) > 0 {
		_spec.Modifiers = vdq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, vdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := vdq.withVexStatements; query != nil {
		if err := vdq.loadVexStatements(ctx, query, nodes, nil,
			func(n *VexDocument, e *VexStatement) { n.Edges.VexStatements = e }); err != nil {
			return nil, err
		}
	}
	if query := vdq.withStatement; query != nil {
		if err := vdq.loadStatement(ctx, query, nodes, nil,
			func(n *VexDocument, e *Statement) { n.Edges.Statement = e }); err != nil {
			return nil, err
		}
	}
	for i := range vdq.loadTotal {
		if err := vdq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (vdq *VexDocumentQuery) loadVexStatements(ctx context.Context, query *VexStatementQuery, nodes []*VexDocument, init func(*VexDocument), assign func(*VexDocument, *VexStatement)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*VexDocument)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	query.withFKs = true
	query.Where(predicate.VexStatement(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(vexdocument.VexStatementsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.vex_document_vex_statements
		if fk == nil {
			return fmt.Errorf(`foreign-key "vex_document_vex_statements" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "vex_document_vex_statements" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (vdq *VexDocumentQuery) loadStatement(ctx context.Context, query *StatementQuery, nodes []*VexDocument, init func(*VexDocument), assign func(*VexDocument, *Statement)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*VexDocument)
	for i := range nodes {
		if nodes[i].statement_vex_documents == nil {
			continue
		}
		fk := *nodes[i].statement_vex_documents
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(statement.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "statement_vex_documents" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (vdq *VexDocumentQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := vdq.querySpec()
	if len(vdq.modifiers) > 0 {
		_spec.Modifiers = vdq.modifiers
	}
	_spec.Node.Columns = vdq.ctx.Fields
	if len(vdq.ctx.Fields) > 0 {
		_spec.Unique = vdq.ctx.Unique != nil && *vdq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, vdq.driver, _spec)
}

func (vdq *VexDocumentQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(vexdocument.Table, vexdocument.Columns, sqlgraph.NewFieldSpec(vexdocument.FieldID, field.TypeInt))
	_spec.From = vdq.sql
	if unique := vdq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if vdq.path != nil {
		_spec.Unique = true
	}
	if fields := vdq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, vexdocument.FieldID)
		for i := range fields {
			if fields[i] != vexdocument.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := vdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := vdq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := vdq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := vdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (vdq *VexDocumentQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(vdq.driver.Dialect())
	t1 := builder.Table(vexdocument.Table)
	columns := vdq.ctx.Fields
	if len(columns) == 0 {
		columns = vexdocument.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if vdq.sql != nil {
		selector = vdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if vdq.ctx.Unique != nil && *vdq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range vdq.predicates {
		p(selector)
	}
	for _, p := range vdq.order {
		p(selector)
	}
	if offset := vdq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := vdq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// VexDocumentGroupBy is the group-by builder for VexDocument entities.
type VexDocumentGroupBy struct {
	selector
	build *VexDocumentQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (vdgb *VexDocumentGroupBy) Aggregate(fns ...AggregateFunc) *VexDocumentGroupBy {
	vdgb.fns = append(vdgb.fns, fns...)
	return vdgb
}

// Scan applies the selector query and scans the result into the given value.
func (vdgb *VexDocumentGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, vdgb.build.ctx, "GroupBy")
	if err := vdgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VexDocumentQuery, *VexDocumentGroupBy](ctx, vdgb.build, vdgb, vdgb.build.inters, v)
}

func (vdgb *VexDocumentGroupBy) sqlScan(ctx context.Context, root *VexDocumentQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(vdgb.fns))
	for _, fn := range vdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*vdgb.flds)+len(vdgb.fns))
		for _, f := range *vdgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*vdgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vdgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// VexDocumentSelect is the builder for selecting fields of VexDocument entities.
type VexDocumentSelect struct {
	*VexDocumentQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (vds *VexDocumentSelect) Aggregate(fns ...AggregateFunc) *VexDocumentSelect {
	vds.fns = append(vds.fns, fns...)
	return vds
}

// Scan applies the selector query and scans the result into the given value.
func (vds *VexDocumentSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, vds.ctx, "Select")
	if err := vds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VexDocumentQuery, *VexDocumentSelect](ctx, vds.VexDocumentQuery, vds, vds.inters, v)
}

func (vds *VexDocumentSelect) sqlScan(ctx context.Context, root *VexDocumentQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(vds.fns))
	for _, fn := range vds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*vds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
