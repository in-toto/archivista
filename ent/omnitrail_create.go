// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/in-toto/archivista/ent/attestation"
	"github.com/in-toto/archivista/ent/mapping"
	"github.com/in-toto/archivista/ent/omnitrail"
)

// OmnitrailCreate is the builder for creating a Omnitrail entity.
type OmnitrailCreate struct {
	config
	mutation *OmnitrailMutation
	hooks    []Hook
}

// SetID sets the "id" field.
func (oc *OmnitrailCreate) SetID(u uuid.UUID) *OmnitrailCreate {
	oc.mutation.SetID(u)
	return oc
}

// SetNillableID sets the "id" field if the given value is not nil.
func (oc *OmnitrailCreate) SetNillableID(u *uuid.UUID) *OmnitrailCreate {
	if u != nil {
		oc.SetID(*u)
	}
	return oc
}

// AddMappingIDs adds the "mappings" edge to the Mapping entity by IDs.
func (oc *OmnitrailCreate) AddMappingIDs(ids ...uuid.UUID) *OmnitrailCreate {
	oc.mutation.AddMappingIDs(ids...)
	return oc
}

// AddMappings adds the "mappings" edges to the Mapping entity.
func (oc *OmnitrailCreate) AddMappings(m ...*Mapping) *OmnitrailCreate {
	ids := make([]uuid.UUID, len(m))
	for i := range m {
		ids[i] = m[i].ID
	}
	return oc.AddMappingIDs(ids...)
}

// SetAttestationID sets the "attestation" edge to the Attestation entity by ID.
func (oc *OmnitrailCreate) SetAttestationID(id uuid.UUID) *OmnitrailCreate {
	oc.mutation.SetAttestationID(id)
	return oc
}

// SetAttestation sets the "attestation" edge to the Attestation entity.
func (oc *OmnitrailCreate) SetAttestation(a *Attestation) *OmnitrailCreate {
	return oc.SetAttestationID(a.ID)
}

// Mutation returns the OmnitrailMutation object of the builder.
func (oc *OmnitrailCreate) Mutation() *OmnitrailMutation {
	return oc.mutation
}

// Save creates the Omnitrail in the database.
func (oc *OmnitrailCreate) Save(ctx context.Context) (*Omnitrail, error) {
	oc.defaults()
	return withHooks(ctx, oc.sqlSave, oc.mutation, oc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (oc *OmnitrailCreate) SaveX(ctx context.Context) *Omnitrail {
	v, err := oc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (oc *OmnitrailCreate) Exec(ctx context.Context) error {
	_, err := oc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (oc *OmnitrailCreate) ExecX(ctx context.Context) {
	if err := oc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (oc *OmnitrailCreate) defaults() {
	if _, ok := oc.mutation.ID(); !ok {
		v := omnitrail.DefaultID()
		oc.mutation.SetID(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (oc *OmnitrailCreate) check() error {
	if _, ok := oc.mutation.AttestationID(); !ok {
		return &ValidationError{Name: "attestation", err: errors.New(`ent: missing required edge "Omnitrail.attestation"`)}
	}
	return nil
}

func (oc *OmnitrailCreate) sqlSave(ctx context.Context) (*Omnitrail, error) {
	if err := oc.check(); err != nil {
		return nil, err
	}
	_node, _spec := oc.createSpec()
	if err := sqlgraph.CreateNode(ctx, oc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(*uuid.UUID); ok {
			_node.ID = *id
		} else if err := _node.ID.Scan(_spec.ID.Value); err != nil {
			return nil, err
		}
	}
	oc.mutation.id = &_node.ID
	oc.mutation.done = true
	return _node, nil
}

func (oc *OmnitrailCreate) createSpec() (*Omnitrail, *sqlgraph.CreateSpec) {
	var (
		_node = &Omnitrail{config: oc.config}
		_spec = sqlgraph.NewCreateSpec(omnitrail.Table, sqlgraph.NewFieldSpec(omnitrail.FieldID, field.TypeUUID))
	)
	if id, ok := oc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = &id
	}
	if nodes := oc.mutation.MappingsIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   omnitrail.MappingsTable,
			Columns: []string{omnitrail.MappingsColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(mapping.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := oc.mutation.AttestationIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2O,
			Inverse: true,
			Table:   omnitrail.AttestationTable,
			Columns: []string{omnitrail.AttestationColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(attestation.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.attestation_omnitrail = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// OmnitrailCreateBulk is the builder for creating many Omnitrail entities in bulk.
type OmnitrailCreateBulk struct {
	config
	err      error
	builders []*OmnitrailCreate
}

// Save creates the Omnitrail entities in the database.
func (ocb *OmnitrailCreateBulk) Save(ctx context.Context) ([]*Omnitrail, error) {
	if ocb.err != nil {
		return nil, ocb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(ocb.builders))
	nodes := make([]*Omnitrail, len(ocb.builders))
	mutators := make([]Mutator, len(ocb.builders))
	for i := range ocb.builders {
		func(i int, root context.Context) {
			builder := ocb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*OmnitrailMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ocb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ocb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ocb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (ocb *OmnitrailCreateBulk) SaveX(ctx context.Context) []*Omnitrail {
	v, err := ocb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ocb *OmnitrailCreateBulk) Exec(ctx context.Context) error {
	_, err := ocb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ocb *OmnitrailCreateBulk) ExecX(ctx context.Context) {
	if err := ocb.Exec(ctx); err != nil {
		panic(err)
	}
}
